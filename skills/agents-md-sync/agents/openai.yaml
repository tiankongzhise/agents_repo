interface:
  display_name: "AGENTS.md Sync"
  short_description: "Sync shared AGENTS.md rules only"
  default_prompt: "Use $agents-md-sync to sync this project's root AI-facing AGENTS.md with the central rules repository, request/persist publish authorization before pushing, publish only AGENTS.md to the owner/repo branch, and treat gh permission errors in Codex sandbox as possible sandbox issues before judging auth."
